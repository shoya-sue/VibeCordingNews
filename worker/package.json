{
  "name": "newsai-vibecording-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register-commands": "node src/register-commands.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
